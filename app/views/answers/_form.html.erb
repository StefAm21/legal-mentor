<%= simple_form_for [@question, @answer] do |f| %>
  <%= f.input :price, label: "$ Precio", required: true %>
  <%= f.input :comment, label: "Comentario", required: true %>
  <%= f.input :avg_time, label: "Tiempo estimado de respuesta:", required: true %>
  <%= f.button :submit, class: "success-btn", value:"Publicar propuesta" %>
<% end %>

